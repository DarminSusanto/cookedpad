apiVersion: apps/v1
kind: Deployment
metadata:
  name: cookedpad-recipes
  namespace: cookedpad
  labels:
    app: cookedpad-recipes
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cookedpad-recipes
  template:
    metadata:
      labels:
        app: cookedpad-recipes
    spec:
      containers:
      - name: recipes
        image: darminsusanto/cookedpad-recipes:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3003
          name: http
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /api/health
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/health
            port: 3003
          initialDelaySeconds: 5
          periodSeconds: 5
