apiVersion: apps/v1
kind: Deployment
metadata:
  name: cookedpad-auth
  namespace: cookedpad
  labels:
    app: cookedpad-auth
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cookedpad-auth
  template:
    metadata:
      labels:
        app: cookedpad-auth
    spec:
      containers:
      - name: auth
        image: darminsusanto/cookedpad-auth:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3001
          name: http
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /api/health
            port: 3001
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/health
            port: 3001
          initialDelaySeconds: 5
          periodSeconds: 5
