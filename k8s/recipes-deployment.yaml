---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cookedpad-recipes
  namespace: cookedpad
  labels:
    app: cookedpad-recipes
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cookedpad-recipes
  template:
    metadata:
      labels:
        app: cookedpad-recipes
    spec:
      serviceAccountName: cookedpad-sa
      containers:
      - name: recipes
        image: darminsusanto/cookedpad-recipes:v1
        imagePullPolicy: Always
        ports:
        - containerPort: 3003
          name: http
        env:
        - name: PORT
          value: "3003"
        - name: NODE_ENV
          value: "production"
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: cookedpad-recipes
  namespace: cookedpad
  labels:
    app: cookedpad-recipes
spec:
  type: ClusterIP
  ports:
  - port: 3003
    targetPort: 3003
    protocol: TCP
    name: http
  selector:
    app: cookedpad-recipes
