---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cookedpad-auth
  namespace: cookedpad
  labels:
    app: cookedpad-auth
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cookedpad-auth
  template:
    metadata:
      labels:
        app: cookedpad-auth
    spec:
      serviceAccountName: cookedpad-sa
      containers:
      - name: auth
        image: darminsusanto/cookedpad-auth:v1
        imagePullPolicy: Always
        ports:
        - containerPort: 3001
          name: http
        env:
        - name: PORT
          value: "3001"
        - name: NODE_ENV
          value: "production"
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: cookedpad-auth
  namespace: cookedpad
  labels:
    app: cookedpad-auth
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: cookedpad-auth
